# for macOS - kills notification center notifications
# doesn't kill push notifications like emails etc
# instead kills overload of system notifications like "disk not ejected"
killall NotificationCenter
